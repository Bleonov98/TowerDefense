#version 400 core
out vec4 FragColor;

in vec2 TexCoords;
in vec3 Colours;

uniform sampler2D texture_diffuse1;

void main()
{   
    vec4 texColor = texture(texture_diffuse1, TexCoords);
    vec3 finalColor = texColor.rgb * Colours;
    FragColor = vec4(finalColor, texColor.a);
}